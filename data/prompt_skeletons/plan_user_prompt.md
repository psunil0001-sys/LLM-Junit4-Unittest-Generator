SOURCE FILE (absolute): {abs_source}
{test_line}
{status_line}
PLAN OUTPUT FILE (absolute, Write/Edit only this path): {plan_output}

Frontmatter mocking_lane MUST be: {plan_lane}.
Frontmatter recipe_id MUST be: {recipe_id}.
Frontmatter recipe_lane MUST be: {recipe_lane}.

{slice_source}

{target_excerpt}
